version: '3.8'

services: 
  postgres:
    image: postgres:latest
    container_name: inotives_postgres
    environment: 
      POSTGRES_USER: inotives # should move this to env
      POSTGRES_PASSWORD: postgrespwd # should move this to env
      POSTGRES_DB: inotives # should move this to env
    ports:
      - "5433:5432" # Change the host port to 5433
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - my_network
volumes:
  postgres_data:

networks:
  my_network: